# Public HTTP port on the Docker host.
HOST_PORT=3039
# Optional comma-separated public origins. Empty enforces matching Origin/Host.
# ALLOWED_ORIGINS=https://geotandem.example.com
ALLOWED_ORIGINS=
# Local backend only; the container always listens on 3039.
PORT=3039
